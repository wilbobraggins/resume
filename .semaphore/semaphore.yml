version: v1.0
name: Deploy website
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Deploy
    task:
      secrets:
        - name: aws user will
      jobs:
        - name: Copy to S3
          commands:
            - 's3cmd sync ~/resume-portfolio --skip-existing s3://williamresume'
