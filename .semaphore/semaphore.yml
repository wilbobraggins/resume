version: v1.0
name: Deploy website
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Deploy
    task:
      secrets:
        - name: aws-secret
      jobs:
        - name: Copy to S3
          commands:
            - checkout
            - 'aws s3 sync "/home/semaphore/resume" "s3://williamresume"'
